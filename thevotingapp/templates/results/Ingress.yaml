apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: results-tva
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host: results.softonic.io
    http:
      paths:
      - backend:
          serviceName: results-tva
          servicePort: 80
        path: /
